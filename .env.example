# Optional poll interval override. Runtime configuration normally lives in the
# platform user-config directory created by the application.
ASM_POLL_INTERVAL=30s

# Optional full path to a local YAML configuration file.
ASM_CONFIG=
