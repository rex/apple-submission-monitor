# AGENTS — Speech

- Invoke the configured `say` command with only the announcement text.
- Never add a voice flag; the operating-system default voice is required.
- Speech failure is non-fatal and must not stop monitoring.
- Templates receive typed transition fields, not raw upstream data.
