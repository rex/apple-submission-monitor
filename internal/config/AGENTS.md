# AGENTS — Configuration

- Keep shipped defaults public-safe and credential-free.
- Generated local configuration uses private directory and file permissions.
- Validate durations and worker limits before starting the TUI.
- Announcement templates are user-editable and must parse at startup.
