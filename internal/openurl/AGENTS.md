# AGENTS — URL opening

- Accept only absolute HTTPS links produced by typed `asc status` fields.
- Invoke the configured opener directly with the URL as its sole argument.
- Return sanitized errors and keep failures non-fatal.
