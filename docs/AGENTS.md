# AGENTS — Documentation

- Use synthetic examples only; never paste live `asc` output or identifiers.
- Keep commands copyable and links current.
- Update operations guidance whenever polling, persistence, controls, or
  configuration behavior changes.
